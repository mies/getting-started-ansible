box: wouter/ubuntu12.04-ansbile@0.0.1
build:
  steps:
    - script:
        name: install ntp
        code: |
          # sudo ansible-playbook -v site.yml -i inventory.ini hosts -k -c local --ask-sudo-pass
          ansible all -m ping --ask-pass -i inventory.ini
